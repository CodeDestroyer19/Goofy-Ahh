import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.goofy_ahh',
  appName: 'goofy_ahh',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
